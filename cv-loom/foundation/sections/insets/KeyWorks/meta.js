export default {
  title: 'Key Works',
  description:
    'Block-level inset that renders a short curated bibliography. Placed on its own line between paragraphs, e.g. ![](@KeyWorks){ids=origin-1859,descent-1871,variation-1868}. Each entry links to the full Publications bibliography entry (via the data-bookmark anchors that Publications emits). The same pipeline as the Publications section — citestyle APA via to-csl — just over a curated id list.',
  inset: true,
}
