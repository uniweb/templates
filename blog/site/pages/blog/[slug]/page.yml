id: de665176
# Dynamic article page
# The [slug] folder name makes this a dynamic route.
# Routes are generated for each item in parent's fetched data.
# Each instance receives:
#   - content.data.articles - single-element array holding the matched article
#     (read it as content.data.articles[0]; [] when nothing matches)

title: Article
description: Blog article
