id: de665176
# The article page — a parametric page.
# The [slug] folder gives it one URL for each record of its route query:
# the parent page's `articles` query. On each URL, its sections receive:
#   - content.data.articles - a list of one, the article the URL names
#     (read it as content.data.articles[0]; [] when nothing matches)

title: Article
description: Blog article
