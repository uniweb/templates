/**
 * Effects Extension Configuration
 *
 * Extensions are purely additive — they contribute section types but
 * don't define theme variables, Layout, or base styles.
 */
export default {
  extension: true
}
