name: {{projectName}}
defaultLanguage: en

# Foundation to use for this site
foundation: foundation

# Content collections
# Markdown files in library/ are processed into JSON at build time
collections:
  articles:
    path: library/articles
    sort: date desc

# Build options
build:
  prerender: true
