/**
 * ReportLayout — the frame for a faculty annual report docusite.
 *
 * A docusite is a document-with-a-URL, not a website. The layout
 * has no named content areas (no header/footer/nav) — it wraps the
 * page body in a <DocumentProvider> and renders a floating Download
 * button that compiles the registered sections to a .docx Blob.
 */
export default {
  title: 'Report Layout',
  description: 'Document-first layout for docusite reports.',
  areas: [],
}
